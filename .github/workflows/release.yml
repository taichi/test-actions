name: Release
on:
  pull_request_review:
    types: [ submitted ]
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - run: echo $GITHUB_SHA
      - run: echo $GITHUB_REF
      - run: echo "${{ toJSON(github.event) }}"
